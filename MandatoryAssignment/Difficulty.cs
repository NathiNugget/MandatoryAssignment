namespace MandatoryAssignment
{
    /// <summary>
    /// Enum for how much difficulty of the application, currently only for how much damage is done
    /// </summary>
    public enum Difficulty
    {
        Novice = 1,
        Normal = 2,
        Trained = 3
    }
}
